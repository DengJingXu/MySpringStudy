<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dengjingxu.mapper.LoginMapper">
    <select id="findStuentByNameByPassword" resultType="com.dengjingxu.domain.Student">
        select * from student where student_name=#{param1} and password=#{param2}
    </select>
</mapper>